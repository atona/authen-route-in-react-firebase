import React from "react";
import Typography from "@material-ui/core/Typography";

const Public = () => (
  <Typography variant="h4">Public Route, anyone can see this page.</Typography>
);

export default Public;
